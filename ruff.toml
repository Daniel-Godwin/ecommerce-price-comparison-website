line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
# FastAPI's dependency-injection idiom requires Depends() in defaults
"app/api/routes.py" = ["B008"]
