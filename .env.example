# Copy to .env and adjust. All variables are optional in Phase 1.
DEFAULT_REGION=NG
CACHE_TTL_SECONDS=1800
REQUEST_TIMEOUT_SECONDS=10
MAX_RETRIES=3
MAX_WORKERS=8

# Phase 3 (not used yet — reserved per design doc Appendix A)
# ANTHROPIC_API_KEY=
# LLM_DAILY_BUDGET_USD=2.00
